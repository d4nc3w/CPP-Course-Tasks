/* Create a function called sortBiggerHalf(), which will accept a vector of numbers as its argument.
The function should check which half of said vector contains elements with greater sum. Sort that half. */

#include <iostream>
#inlcude <vector>

auto main() -> int {
    //...
}
